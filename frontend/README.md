# Frontend

See the [repo-root README](../README.md) for setup, architecture, and design decisions — this project is one half of that write-up, not documented separately.

Quick start: `npm install && npm run dev` (needs the backend running — see root README's Setup section).
