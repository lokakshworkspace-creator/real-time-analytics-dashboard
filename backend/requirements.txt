fastapi>=0.115,<0.116
uvicorn[standard]>=0.32,<0.33
motor>=3.6,<3.7
pydantic>=2.9,<3.0
pydantic-settings>=2.6,<3.0
python-dotenv>=1.0,<2.0

# simulator.py only (standalone HTTP client, no other backend module needs it)
requests>=2.32,<3.0

# detectors/isolation_forest.py only (Phase 5b stretch detector — never
# imported by the live ingest path, see that module's docstring)
scikit-learn>=1.5,<1.7

# tests/ only
pytest>=8.3,<9.0
httpx>=0.27,<0.29  # required by fastapi.testclient.TestClient
